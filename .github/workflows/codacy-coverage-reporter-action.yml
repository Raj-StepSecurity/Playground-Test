name: codacy-coverage-reporter-action

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  codacy-coverage-reporter-action-main:
    name: Codacy Coverage Reporter (main)
    runs-on: ubuntu-latest
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@v2
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@v6

      - name: Create coverage report
        run: |
          mkdir -p coverage
          cat > coverage/coverage.xml << 'EOF'
          <?xml version="1.0" ?>
          <coverage line-rate="0.85" lines-covered="85" lines-valid="100" version="5.5">
            <packages><package name="src" line-rate="0.85"><classes/></package></packages>
          </coverage>
          EOF

      - name: Upload Coverage to Codacy
        uses: step-security/codacy-coverage-reporter-action@main
        with:
          project-token: "49CVZHbGyYRvAKw08VIQ"
          coverage-reports: "coverage/coverage.xml"
